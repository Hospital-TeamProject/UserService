logging:
  level:
    root: INFO
spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/user_db
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create

server:
  port: 8081
  servlet:
    context-path: "/api"


